# spine-layout-tests

**Live demo:** https://cyberdex.github.io/spine-layout-tests/

A PixiJS + Spine layout test harness. 

## Development

```bash
pnpm install
pnpm dev      # local dev server on http://localhost:8080
pnpm build    # production build into dist/
```
